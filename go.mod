module netnaval

go 1.22

require github.com/wailsapp/wails/v2 v2.8.0

// Ejecuta `go mod tidy` para generar go.sum con todas las dependencias
// transitivas de Wails v2 antes de la primera compilación.
